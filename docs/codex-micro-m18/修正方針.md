# 修正方針: Codex Micro互換M18入力面

## 維持するOSS実装

- `dazer1234/codex-stream-deck`の`DeckController`、`CodexMicroRendererBridge`、描画、Codexイベント形式は変更しない。
- M18 HIDアクセスは`mirajazz 0.9.0`を使う既存アダプターを維持する。
- Codex Desktop本体、M18ファームウェア、USBドライバーには変更を加えない。

## 最小修正

1. `src/m18.ts`の物理キー割当をCodex Microの入力一覧に合わせる。
2. Back、Forward、Sidebarは既存の`sendJoystick`を使う。
3. Reasoningの回転相当は既存の`adjustReasoning`を1タップ1段として使う。
4. エンコーダ押下は既存の`sendEncoder(1/0)`を使い、下段中央の500ms長押しに割り当てる。
5. 短押し／長押し判定だけを独立した小さなヘルパーとして追加し、単体テストする。
6. 実機入力の確認用に、受信イベントを`%LOCALAPPDATA%\CodexDeck\m18-events.log`へ記録する。

New TaskなどCodex Microに存在しない独自操作は既定割当から除外する。
