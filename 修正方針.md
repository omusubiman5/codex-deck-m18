# 修正方針

## 原則

Codex Deckを作り直さない。Elgato Stream Deck SDKに接している表面だけを抽象化し、M18用の表面実装を追加する。

## Codex Deck側

1. `DeckController`からElgatoの`KeyAction`、logger、global settingsへの直接依存を除く。
2. 最小インターフェース`DeckSurfaceAction`と`DeckRuntime`を導入する。
3. 従来のStream DeckプラグインではElgato SDKを`DeckRuntime`へ渡し、従来動作を保つ。
4. M18専用エントリーポイントでは15個のLCD surfaceを登録し、選択中の環境に応じて既存の`DeckController`公開操作へ入力を配送する。
5. 既存のCDP、relay、描画、Codexイベント送信ロジックは変更しない。

## 環境切替

1. 下段3ボタンはページ内Bindingへ渡さず、環境選択として最初に処理する。
2. 環境1と2の切替時は、旧surface登録を解除してから同じ15個のLCD surfaceへ新しい既存機能を登録する。
3. 選択環境は`%LOCALAPPDATA%\CodexDeck\m18-environment`へ保存する。
4. 環境3ではadapterへ既存の`shutdown`を送り、runtimeを終了する。
5. watcherは環境3の間だけVSD Craftを起動し、環境1・2へ戻るまでCodex runtimeを再起動しない。
6. VSD Craftから戻る操作には同梱PowerShellを使い、新しい常駐サービスやHIDドライバーを追加しない。

## M18 adapter側

1. Rust製の小型stdioプロセスとして分離する。
2. HID列挙、接続、画像出力、keep-aliveには`mirajazz`を直接使う。
3. Codex Deckとの通信は1行1JSONの小さなプロトコルに限定する。
4. Codex DeckのSVG data URLをadapter内でラスター化してから、`mirajazz`へ渡す。
5. OpenDeck/OpenAction固有のデバイス登録、WebSocket、プラグイン管理は持ち込まない。

## 互換性・ライセンス

- 上流Codex DeckのMITライセンスと履歴を保持する。
- `mirajazz`はMPL-2.0依存として利用する。
- M18の定数・入力対応について参照した`opendeck-m18`をNOTICEへ明記する。
- 配布時は各依存のライセンス本文を同梱する。

## 対象外

- 汎用Stream Deckプロファイル編集UI
- OpenDeckプラグイン互換ホスト
- M18以外の機種対応
- Codex Desktop内部HIDとしての偽装
- VSD Craftの設定形式の独自解析・書換え
- M18ファームウェア更新
