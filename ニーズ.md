# ニーズ

## 目的

Windows版Codex Desktopを、接続済みのVSD Inside M18から操作する。M18の15個のLCDキーにはCodexの状態を表示し、全18ボタンからCodex操作を実行できるようにする。

## 採用構成

```text
Codex Desktop
      ↕ CDP（localhostのみ）
Codex Deck（既存のCodex制御・状態取得・描画を維持）
      ↕ JSON Lines / stdio
M18 adapter（mirajazzを直接利用）
      ↕ USB HID
VSD Inside M18（VID 0x5548 / PID 0x1000）
```

## 必須要件

- OpenDeckを実行時依存にしない。
- Codex DeckのCDP接続、Codex Microイベント変換、状態管理、SVG描画を維持する。
- M18制御は既存OSSの`mirajazz`を利用し、独自HIDプロトコル実装を増やさない。
- M18の実機識別子`0x5548:0x1000`、usage page `0xFFA0`、usage `1`を使用する。
- 15 LCDキーへ64×64表示を出力する。
- 15 LCDキーと3底面ボタンの押下・解放をCodex操作へ対応付ける。
- Codex DesktopまたはM18が未接続でも、原因をログへ出して安全に終了できる。
- 設定、キャッシュ、ログ以外はユーザー領域を変更しない。

## 初期ボタン配置

| M18位置 | 操作 |
|---|---|
| LCD 1～6 | Agent 1～6 |
| LCD 7 | Fast |
| LCD 8 | Approve |
| LCD 9 | Reject |
| LCD 10 | Fork |
| LCD 11 | Dictation |
| LCD 12 | Send |
| LCD 13 | Plan |
| LCD 14 | Reasoning down |
| LCD 15 | Reasoning up |
| Bottom left | Back |
| Bottom middle | New task |
| Bottom right | Sidebar |

## 制約

- Codex Desktop本体を改変しない。
- M18のファームウェアを書き換えない。
- 仮想HIDドライバーを追加しない。
- `opendeck-m18`のGPLコードを無断でMIT部分へ混在させず、由来とライセンスを明記する。
