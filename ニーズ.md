# ニーズ

## 目的

Windows版Codex Desktopを、接続済みのVSD Inside M18から操作する。15個のLCDキーは選択中の環境に応じて機能を切り替え、下段3ボタンは常に環境選択だけに使用する。

## 採用構成

```text
Codex Desktop
      ↕ CDP（localhostのみ）
Codex Deck（既存のCodex制御・状態取得・描画を維持）
      ↕ JSON Lines / stdio
M18 adapter（mirajazzを直接利用）
      ↕ USB HID
VSD Inside M18（VID 0x5548 / PID 0x1000）
```

## 必須要件

- OpenDeckを実行時依存にしない。
- Codex DeckのCDP接続、Codex Microイベント変換、状態管理、SVG描画を維持する。
- M18制御は既存OSSの`mirajazz`を利用し、独自HIDプロトコル実装を増やさない。
- M18の実機識別子`0x5548:0x1000`、usage page `0xFFA0`、usage `1`を使用する。
- 15 LCDキーへ64×64表示を出力する。
- 下段左・中央・右を、それぞれ環境1・2・3の専用切替ボタンにする。
- 下段3ボタンへCodex操作やマクロを重ねて割り当てない。
- 環境1はCodex Micro、環境2はCodex Deckの既存追加機能、環境3はメーカー標準アプリVSD Craftとする。
- 環境3を選ぶとCodex DeckはM18をshutdownして制御用HIDを解放し、VSD Craftへ渡す。
- Codex DesktopまたはM18が未接続でも、原因をログへ出して安全に終了できる。
- 設定、キャッシュ、ログ以外はユーザー領域を変更しない。

## 環境ボタン

| M18位置 | 環境 |
|---|---|
| 下段左 | 環境1：Codex Micro |
| 下段中央 | 環境2：Codex Deck追加機能 |
| 下段右 | 環境3：VSD CraftによるM18標準機能 |

## 環境1：Codex Micro

| M18位置 | 操作 |
|---|---|
| LCD 1～6 | Agent 1～6 |
| LCD 7 | Fast |
| LCD 8 | Approve |
| LCD 9 | Reject |
| LCD 10 | Fork |
| LCD 11 | Dictation |
| LCD 12 | Send |
| LCD 13 | Plan |
| LCD 14 | Back |
| LCD 15 | Forward |

## 環境2：Codex Deck追加機能

| M18位置 | 既存Codex Deck機能 |
|---|---|
| LCD 1 | Sidebar |
| LCD 2 | Encoder press |
| LCD 3～4 | Reasoning down / up |
| LCD 5 | New task |
| LCD 6～8 | Usage overview / 5-hour / weekly |
| LCD 9 | Reset Credit（1.2秒保持） |
| LCD 10 | Windows / Mac target |
| LCD 11～15 | Codex / Terminal / Diff / Browser / Settings |

## 環境3：M18標準

- WindowsへインストールしたVSD Craftを使用する。M18本体へアプリをインストールしない。
- Codex Deck M18 runtimeとVSD Craftは同時にM18制御用HIDを保持しない。
- VSD Craft側の下段左・中央には同梱の環境切替スクリプトを割り当て、環境1・2へ戻せるようにする。

## 制約

- Codex Desktop本体を改変しない。
- M18のファームウェアを書き換えない。
- 仮想HIDドライバーを追加しない。
- VSD Craftのファームウェア更新機能は環境切替の実装では使用しない。
- `opendeck-m18`のGPLコードを無断でMIT部分へ混在させず、由来とライセンスを明記する。
