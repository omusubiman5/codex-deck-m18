# 実行方針

## 実施順序

1. 上流Codex Deckのテストを変更前基準として確認する。
2. ハードウェア非依存のsurface/runtime境界を追加する。
3. M18 adapterとJSON Linesプロトコルを追加する。
4. 15 LCDキーの環境1・2 Bindingと、下段3ボタンの環境選択を実装する。
5. 環境3のHID解放、状態保存、VSD Craft起動待機をwatcherへ実装する。
6. TypeScript型検査、既存単体テスト、Stream Deck版ビルドを実行する。
7. Rust adapterのformat、lint、test、release buildを実行する。
8. `--probe`で読取り中心の実機列挙・接続確認を行う。
9. 実機で環境1・2の再描画と、環境3選択時のadapter終了を確認する。

## 合格条件

- 既存Codex Deckのテストが退行しない。
- Stream Deck版も引き続きビルドできる。
- M18 adapterが`0x5548:0x1000`を一意に検出する。
- LCD画像1枚をエラーなく送信できる。
- ボタン押下・解放が正しい0～17のindexとして届く。
- OpenDeckプロセスなしで動作する。
- 下段3ボタンがCodex操作を送らず、環境選択だけを行う。
- 環境3ではadapterがM18を解放し、watcherがCodex runtimeを再取得しない。
- VSD Craft未導入時は環境3へ滞留せず、ログを残して環境1へ安全に戻る。

## 安全策

- firmware、feature reportの更新系操作は実装しない。
- adapterが受理する画像indexを0～14に制限する。
- brightnessを0～100に制限する。
- 不正JSON、不正data URL、不明コマンドはエラー応答し、プロセスをpanicさせない。
- 実機smoke後もファームウェアやWindowsドライバーには変更を残さない。

## 中止条件

- 実機IDが一致しない。
- HIDを別アプリが排他的に保持している。
- `mirajazz`の公開APIだけではM18 protocol 3を扱えない。
- Codex DesktopのCDP起動に既存Codex Deck以上の本体改変が必要になる。

## 2026-08-15 実行結果

- TypeScript型検査: 合格
- Codex Deck単体テスト: 140/140合格（環境ボタン・保存復元・VSD Craft待機・配布同梱を含む）
- 従来Stream Deck版ビルド: 合格
- Rust adapter単体テスト: 2/2合格（MSVC ABI）
- Rust releaseビルド: 合格
- M18 Node runtime再ビルド: 合格
- 環境切替PowerShell構文検査: 合格
- 環境選択保存smoke: 環境2を`m18-environment`へ保存できることを確認
- 実機probe: `HOTSPOTEKUSB HID DEMO`, `0x5548:0x1000`, serial `81D0DA782039`を検出
- 実機LCD smoke: LCD key 1へのSVG画像送信が`ack`で完了
- 入力監視: adapter接続とready応答を確認。今回の監視時間中に物理押下はなかったため、下段3ボタンの実押下は未確認
- VSD Craft: 公式配布EXEは取得済みだがAuthenticodeが`NotSigned`のため、無人インストールは未実施
- 統合起動診断: 現在のCodex DesktopはCDPなしで起動中。作業中のCodex自身を強制終了しないため、完全統合起動は次回の通常再起動後に実施する

未観測の下段入力、VSD Craft上の下段割当、CDP接続後のCodexコマンド配送は未合格として扱い、推測で完了扱いにしない。
